apiVersion: apps/v1
kind: Deployment
metadata:
  name: ichin-memory-engine
  namespace: ichin-system
  labels:
    app.kubernetes.io/name: ichin-memory-engine
    app.kubernetes.io/component: memory-engine
    app.kubernetes.io/part-of: ichin-os
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: ichin-memory-engine
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ichin-memory-engine
        app.kubernetes.io/component: memory-engine
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
      containers:
        - name: memory-engine
          image: ichin/memory-engine:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8013
              protocol: TCP
          envFrom:
            - configMapRef:
                name: ichin-config
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ichin-db-secret
                  key: password
          resources:
            requests:
              cpu: "200m"
              memory: "512Mi"
            limits:
              cpu: "1"
              memory: "2Gi"
          livenessProbe:
            httpGet:
              path: /health
              port: 8013
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8013
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
---
apiVersion: v1
kind: Service
metadata:
  name: ichin-memory-engine
  namespace: ichin-system
  labels:
    app.kubernetes.io/name: ichin-memory-engine
    app.kubernetes.io/component: memory-engine
spec:
  type: ClusterIP
  ports:
    - port: 8013
      targetPort: 8013
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: ichin-memory-engine
