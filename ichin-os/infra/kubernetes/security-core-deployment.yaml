apiVersion: apps/v1
kind: Deployment
metadata:
  name: ichin-security-core
  namespace: ichin-system
  labels:
    app.kubernetes.io/name: ichin-security-core
    app.kubernetes.io/component: security-core
    app.kubernetes.io/part-of: ichin-os
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: ichin-security-core
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ichin-security-core
        app.kubernetes.io/component: security-core
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
      containers:
        - name: security-core
          image: ichin/security-core:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8017
              protocol: TCP
          envFrom:
            - configMapRef:
                name: ichin-config
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 8017
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8017
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
---
apiVersion: v1
kind: Service
metadata:
  name: ichin-security-core
  namespace: ichin-system
  labels:
    app.kubernetes.io/name: ichin-security-core
    app.kubernetes.io/component: security-core
spec:
  type: ClusterIP
  ports:
    - port: 8017
      targetPort: 8017
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: ichin-security-core
